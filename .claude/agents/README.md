# Agents

Deliberately empty.

Add an agent here only after the same multi-step task has been done by hand **three or more
times** — otherwise it is context cost with no payoff. When you do add one, name the repeated
task it replaces in the file itself.
